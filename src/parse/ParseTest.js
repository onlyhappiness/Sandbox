import React from 'react'

// import Parse from "parse/dist/parse.min.js";
// import pasConfig from "../../pasConfig";
// const { parseAppId, parseJavascriptKey, parseMasterKey, parseServerURL } = pasConfig;
// Parse.initialize(parseAppId, parseJavascriptKey, parseMasterKey);
// Parse.serverURL = parseServerURL;

function ParseTest() {
  return (
    <div>
      콘솔 확인 ㄱㄱ
    </div>
  )
}

export default ParseTest
